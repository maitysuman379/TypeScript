// var value: string | Array<string> | number[]

// var value : any 

// value = 21;
// value = "Suman";

// console.log(value.toUpperCase())


function myFunction(){
    console.log("My name is Suman Maity");
}

function Suman():string{
    console.log("Suman")
    return "Suman   "
}

function unDifi():null{
    let s = null
    return s;
}

// function calPrice(item: number, price: number, text ? : string) {
//     if (text !== undefined){
//         return (text + (item*price))
//     }else{
//         return (item*price)
//     }
// }

// const priceInput = prompt("Enter the price:");
// const quantityInput = prompt("Enter the item quantity:");
// const text_prompt = prompt("Type what you want:");

// if (priceInput !== null && quantityInput !== null) {
//     const price = Number(priceInput);
//     const quantity = Number(quantityInput);
//     const text = String(text_prompt);

//     console.log(calPrice(quantity, price, text));
// } else {
//     console.log("Input cancelled.");
// }